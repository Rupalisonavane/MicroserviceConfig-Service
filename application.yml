spring:
  application:
   name: config-default

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
  service-url:
  defaultzone: http://localhost:8761/eureka        


